# Notebooks

These notebooks are for exploratory analysis and figure development.

To run them from the repository root, ensure the helper modules in `scripts/`
are on your Python path, for example:

```
export PYTHONPATH="$(pwd)/scripts"
```
